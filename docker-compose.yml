version: "3.9"

services:
  web:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - scanner
    environment:
      SCANNER_URL: http://scanner:8000

  scanner:
    build: ./scanner
    ports:
      - "8000:8000"
    env_file:
      - ./scanner/.env
