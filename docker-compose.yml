version: "3.9"

services:
  scanner:
    build:
      context: ./scanner
    ports:
      - "8000:8000"
    env_file:
      - ./scanner/.env

  web:
    build:
      context: .
    ports:
      - "3000:3000"
    env_file:
      - ./.env
    depends_on:
      - scanner
